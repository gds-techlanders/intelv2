# This is my first workflow
name: workflow1
on: 
  workflow_dispatch:
  push:
jobs:
  build:
    runs-on: gagan
    steps:
       - name: this is first step
         run: |
           echo $HOSTNAME
           sleep 30
       - name: second step
         run: |
           uname -a
           cat /etc/os-release
           touch /tmp/gagandeep
           sleep 30
       - run: echo "step 3"
      
