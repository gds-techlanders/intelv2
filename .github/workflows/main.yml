# This is my first workflow
name: workflow1
on: 
  workflow_dispatch:
  push:
jobs:
  build:
    runs-on: gagan
    steps:
       - name: this is first step
         run: |
           echo "hello from step1"
           sleep 30
       - name: second step
         run: |
           echo " hello from step2"
           cat /etc/os-release
           sleep 30
       - run: echo "step 3"
      
